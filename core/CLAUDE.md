# Contexte du projet — Migration Flutter → React Native

Ce fichier est lu automatiquement par Claude Code au début de chaque session. Il doit être tenu à jour après chaque phase de travail. Ne le laisse jamais devenir obsolète : une info fausse ici est pire qu'une absence d'info.

## Objectif du projet

Réécrire l'application mobile Flutter en React Native, à l'identique :
- Même design (couleurs, typographie, spacing, disposition)
- Même logique métier (validations, règles, comportements)
- Même backend Laravel, **inchangé, jamais modifié**
- Aucune fonctionnalité ajoutée ou supprimée par rapport à l'app Flutter

## Emplacements

- Projet Flutter d'origine (référence, lecture seule) : `./Flutter APP/User` (package `ovopay`, app mobile financière/wallet)
- Projet Laravel (référence API, lecture seule) : `./core` (routes API mobiles dans `core/routes/api/api.php`, montées sur préfixe `/api`)
- Nouveau projet React Native (à construire) : `./app-react-native`

## Stack technique choisie pour React Native

- Framework : **React Native CLI** (bare) — confirmé par l'utilisateur le 2026-07-28 (pas Expo). Conséquence : les modules natifs (Firebase Messaging, notifications locales riches, biométrie, scanner QR, image/document picker + crop, contacts, secure storage, webview in-app, téléchargement/ouverture de fichiers) devront être intégrés via leurs équivalents RN CLI classiques avec link natif (`react-native-firebase`, `react-native-biometrics` ou `react-native-keychain` biometry, `react-native-vision-camera`/`react-native-camera-kit` pour le QR, `react-native-image-picker`, `react-native-image-crop-picker`, `react-native-contacts`, `react-native-keychain`, `react-native-webview`, etc.) — pas de config-plugins Expo.
- Navigation : React Navigation — `bottom tabs` (4 onglets : Home / Historique / Relevé / Profil, équivalent du `IndexedStack` + `GNav` Flutter) + `stack` par domaine fonctionnel (Auth, chaque module financier étant un mini-wizard en stack) + `drawer` pour le menu latéral (avatar, raccourci QR, logout) — voir `audit-flutter.md` §4 pour le détail des ~30 écrans et 45 routes GetX à reproduire.
- Gestion d'état : Flutter utilise **GetX** en mode `GetxController` + `update()` (pattern impératif simple, pas de flux réactif `.obs`/`Obx`) — pas de store global complexe. Équivalent recommandé côté RN : **Zustand** (state léger par feature, proche du pattern `GetxController` par écran) + **Context API** pour l'état transverse minimal (session utilisateur, thème). Pas besoin de Redux Toolkit vu la simplicité du state management d'origine.
- Client API : axios avec intercepteur pour l'authentification (reproduit `ApiService`/Dio de Flutter : ajout automatique `Authorization: Bearer <token>`, gestion 401 → déconnexion)
- Stockage local : AsyncStorage pour la majorité des données (à l'identique de `shared_preferences`, y compris le token d'accès — voir `audit-flutter.md` §11 point 1 sur le fait que le token n'est pas chiffré côté Flutter, à valider si on reproduit ce choix ou si on l'améliore) ; `react-native-keychain` ou `expo-secure-store` pour reproduire l'usage `flutter_secure_storage` (PIN, solde en cache — le mécanisme PIN a un bug identifié côté Flutter, voir `audit-flutter.md` §11 point 2, à clarifier avant portage)
- Backend : Laravel, communication via **Laravel Sanctum** (confirmé dans `core/composer.json` et `core/routes/api/api.php` : middleware `auth:sanctum` + `token.permission:user_token` custom, token Bearer sur toutes les routes protégées). Aucun mécanisme de refresh token identifié côté Flutter (déconnexion directe sur 401) — à confirmer côté backend avant de décider si le RN doit gérer un refresh ou reproduire ce comportement à l'identique.

## Règles impératives

1. **Ne jamais modifier le dossier Laravel** pour la logique métier, les routes ou les contrôleurs — lecture seule par défaut, uniquement pour comprendre les routes API (`routes/api.php`) et les contrôleurs. **Exceptions ponctuelles validées explicitement par l'utilisateur** (voir journal des décisions pour le détail des fichiers touchés à chaque fois) :
   - `2026-07-28` — renommage de marque « OvoPay » → « Amana », nom affiché uniquement.
   - `2026-07-29` — ajout des passerelles de paiement PawaPay et InTouch dans le module Add Money, suivant le pattern déjà utilisé pour Stripe.
   - `2026-07-30` — refonte complète des templates de notification (contenu + habillage visuel de l'email), voir journal.
   Toute autre modification du dossier Laravel doit repasser par une validation explicite de l'utilisateur avant d'être effectuée.
2. **Ne plus considérer le dossier Flutter d'origine comme lecture seule.** Décision du `2026-07-30` (validée par l'utilisateur) : les deux pistes — app Flutter existante et future réécriture React Native — sont désormais menées **en parallèle**. L'app Flutter (`./Flutter APP/User`) est activement configurée/maintenue comme app de production court terme (rebranding AMANA, connexion au backend réel `fintech.gorapene.com`, Firebase propre — voir journal du 2026-07-30). Le projet React Native (`./app-react-native`) reste à créer ; l'audit et le brief design existants restent valables comme référence pour le jour où ce chantier reprendra.
3. Avant de migrer un écran, toujours relire le fichier Flutter correspondant pour ne rien oublier (états, conditions, messages d'erreur).
4. Réutiliser les composants déjà créés dans `src/components` plutôt que d'en dupliquer.
5. Respecter le thème centralisé dans `src/theme` — ne jamais coder une couleur ou une taille en dur dans un écran.
6. Après chaque lot d'écrans migré, mettre à jour la section "État d'avancement" ci-dessous.

## État d'avancement

> À mettre à jour après chaque phase. Format : `[x]` fait, `[ ]` à faire, `[~]` en cours.

### Phase 0 — Audit
- [x] `audit-flutter.md` généré (2026-07-28) — en attente de validation utilisateur

### Phase 1 — Setup
- [ ] Squelette React Native créé
- [ ] Navigation de base fonctionnelle
- [ ] Client API connecté et testé sur une route réelle
- [ ] Thème (couleurs, typo, spacing) reproduit

### Phase 2 — Écrans migrés
- [ ] Auth (splash, onboard, login/register, biométrie, forgot pin, email verification, 2FA setup/verify, KYC)
- [ ] Dashboard/Home (shell + bottom nav + drawer, home, offres promo)
- [ ] Profil/Paramètres (hub, info, edit, security/change pin, delete account, notifications settings, privacy, app preferences, page content)
- [ ] QR Code (my code, scan, login)
- [ ] Historique (transaction history, statements)
- [ ] Notifications / FAQ / Langue / No internet / Maintenance
- [ ] Support Ticket (liste, nouveau, détails)
- [ ] Modules financiers (12) : Send Money, Request Money, Cash Out, Make Payment, Gift Card, Airtime Recharge, Bill Pay, Bank Transfer, Education Fee, Donation, Microfinance, Investment
- [ ] Add Money (+ webview paiement)
- [ ] Virtual Cards

> Détail complet de chaque écran (fichiers, state, validations, navigation, API) dans `audit-flutter.md` §4.

### Phase 3 — Transverse
- [ ] Notifications push
- [ ] Upload/affichage images
- [ ] Géolocalisation
- [ ] Offline/cache
- [ ] Deep linking

### Phase 4 — QA
- [ ] Checklist QA créée
- [ ] Checklist QA validée

## Décisions techniques prises (journal)

> Ajoute une ligne ici à chaque choix technique important, pour ne pas le redemander/redécider plus tard.

- `[date]` — exemple : "State management : Zustand choisi car l'app Flutter utilisait Provider de façon simple, pas besoin de Redux."
- `2026-07-28` — Audit Flutter/Laravel complet réalisé (`audit-flutter.md`). Auth confirmée = Laravel Sanctum (pas Passport/JWT). State management Flutter confirmé = GetX impératif (`GetxController`/`update()`, pas de flux réactif) → recommandation Zustand + Context API pour le RN (à valider). Plusieurs anomalies/routes mortes détectées côté Flutter/Laravel (token non chiffré, bug clé PIN secure storage, endpoint `request/money-approve` introuvable côté Laravel, méthode `InvestmentRepo.makeInvestmentHistory` buguée, etc.) — voir `audit-flutter.md` §11, à trancher au cas par cas avant portage, ne pas corriger silencieusement.
- `2026-07-28` — Framework RN confirmé par l'utilisateur : **React Native CLI** (bare), pas Expo. Une maquette UX haute-fidélité (12 écrans clés) a été produite en artifact à partir de la palette/typo/patterns de l'audit, sous le nom provisoire « Amana » en anticipation du renommage de marque demandé par l'utilisateur — aucun code (Flutter, Laravel, RN) créé à cette étape, uniquement la maquette visuelle.
- `2026-07-28` — Renommage de marque **OvoPay → Amana** appliqué directement dans le code source Laravel (exception validée par l'utilisateur, périmètre = nom affiché uniquement) :
  - `install/database.sql` : `general_settings.site_name` (`OvoPay` → `Amana`, c'est le nom sitewide utilisé par `general-setting`/API mobile), SEO/social title, contenus CMS de la landing page (`how_to_work.content`, `cta.content`, `testimonial.element`, `faq.element`, `policy_pages.element`, `contact_us.content`) — remplacement littéral `OvoPay` → `Amana` (41 occurrences).
  - `core/app/Http/Helpers/helpers.php` — `systemDetails()['name']` (affiché uniquement sur la page admin « System Info ») : `'ovopay'` → `'Amana'`.
  - `core/resources/lang/{bn,es,fr,it,ja,pt,ru,tr}.json` — mêmes textes marketing traduits (clés ET valeurs) : `OvoPay` → `Amana` (60 occurrences/fichier), pour garder la correspondance de clé de traduction cohérente avec le contenu CMS renommé.
  - **Volontairement non touché à cette étape** (hors périmètre « nom affiché uniquement », à trancher séparément si besoin) : email `support@ovopay.com`, commentaire de nom de base de données dans le dump SQL (`-- Database: \`ovopay\``), logo/assets visuels, palette de couleurs (bleu `#2B5BEE`/or `#EEBE2B` inchangée). Le domaine `ovosolution.com` a ensuite été traité séparément le 2026-07-29 (voir entrée ci-dessous).
  - Pas de logo/couleurs changés (non demandé). Ce changement affecte le fichier d'installation (`install/database.sql`) — il ne se répercute sur une base de données déjà installée que si le site est réinstallé ou si la ligne `general_settings` est mise à jour manuellement/via l'admin.
- `2026-07-29` — Intégration de **PawaPay** et **InTouch** comme nouvelles méthodes « Add Money », en suivant exactement le pattern data-driven déjà utilisé par Stripe/MercadoPago/Flutterwave (voir blueprint établi par audit du code existant : `gateways`/`gateway_currencies` en DB + `App\Http\Controllers\Gateway\<Alias>\ProcessController` résolu dynamiquement par `Gateway\PaymentController::depositConfirm()`, aucun changement nécessaire dans les contrôleurs/routes/models partagés).
  - Fichiers créés : `app/Http/Controllers/Gateway/PawaPay/ProcessController.php`, `app/Http/Controllers/Gateway/InTouch/ProcessController.php`, vues `resources/views/templates/basic/{user,agent}/payment/{PawaPay,InTouch}.blade.php`.
  - Fichier modifié : `routes/ipn.php` (ajout des routes webhook `pawapay`/`in-touch` + routes de polling de statut `pawapay/status/{id}` et `in-touch/status/{trx}`).
  - Seed DB (`install/database.sql`) : lignes `gateways` (code 126 PawaPay, code 127 InTouch) et `gateway_currencies` (XOF pour les deux) — **désactivées par défaut** (`status = 2`) et **sans identifiants** (champs vides), à activer manuellement depuis `admin/gateway/automatic` une fois les vraies clés API saisies — aucun changement de code nécessaire pour ça, le formulaire admin est générique.
  - **Différence de UX par rapport à Stripe** : PawaPay et InTouch ne sont pas des passerelles à page hébergée (pas de redirection vers une page de paiement) — ce sont des push mobile money côté serveur (l'utilisateur confirme sur son téléphone via une invite USSD). La vue de confirmation affiche donc un écran d'attente qui interroge (`polling` JS, toutes les 4s, ~4 min max) une petite route de statut jusqu'à confirmation, puis redirige vers `success_url`/`failed_url` — c'est ce mécanisme qui permet à la WebView React Native existante (qui détecte le succès par pattern d'URL) de fonctionner sans modification.
  - **Points non vérifiés en conditions réelles, à confirmer avant mise en production** :
    - PawaPay : les codes `country`/`correspondent` par corridor (ex. opérateur Orange/MTN par pays) doivent être saisis par l'admin depuis son tableau de bord PawaPay — je n'ai pas la liste exhaustive et fiable de ces codes, donc rien n'est pré-rempli.
    - InTouch : l'URL de base utilisée (`https://apidist.gutouch.net/apidist/sec/{agency_code}/cashin`) est celle documentée pour les pays hors Tanzanie ; à confirmer/ajuster si le corridor cible est la Tanzanie (`api-tz.gutouch.net`) ou un autre pays. Le endpoint utilisé est **« Transfer » (`/cashin`)**, confirmé par l'utilisateur — les endpoints « Direct API »/« Checkout page » du portail InTouch n'ont pas pu être documentés (portail en SPA Angular résistant au scraping automatisé) et n'ont donc pas été utilisés.
    - Format exact du numéro de téléphone (`$user->mobile`) attendu par InTouch (avec/sans zéro initial) non testé en conditions réelles — normalisation défensive appliquée (`ltrim($user->mobile, '0')`) mais à vérifier avec de vraies données utilisateur.
    - Vérification de signature de callback non implémentée pour aucune des deux passerelles (PawaPay la propose en option via RFC-9421, non documentée assez précisément pour l'implémenter fiablement ; InTouch ne documente pas de mécanisme de signature) — la protection actuelle se limite à l'idempotence déjà présente dans `PaymentController::userDataUpdate()` (un webhook rejoué ne peut pas créditer deux fois), ce qui est acceptable mais pas équivalent à une vérification de signature.
- `2026-07-29` — Remplacement du domaine **ovosolution.com → amanagroupe.com** sur le site (même périmètre « nom affiché »/contenu, exception validée par l'utilisateur) : `install/database.sql` (lien de téléchargement APK dans `cta.content`, email `info@ovosolution.com` devenu l'adresse d'expédition système `general_settings.email_from`, mention `support@ovosolution.com` dans un contenu FAQ/CMS, `src` d'image logo dans un template d'email) + mêmes textes dans les 8 fichiers `core/resources/lang/{bn,es,fr,it,ja,pt,ru,tr}.json`. Remplacement littéral du domaine uniquement (pas des segments de chemin `/ovopay/...`, non demandés).
  - **À vérifier par l'utilisateur** : `info@ovosolution.com` devient l'adresse `From` réelle des emails système (`general_settings.email_from`) — si `amanagroupe.com` n'a pas de boîte `info@` ni d'enregistrements SPF/DKIM configurés, les emails sortants risquent d'échouer ou d'arriver en spam. Le `src` d'image dans le template d'email (`preview.amanagroupe.com/ovopay/demo/assets/images/logo_icon/logo.png`) pointait vers une image hébergée par le vendeur d'origine — le changement de domaine seul ne fera pas apparaître une image réelle à cette adresse sur `amanagroupe.com` sauf si ce chemin exact y est effectivement hébergé ; à remplacer par un vrai logo hébergé si besoin.
  - Volontairement non touché : `support@ovopay.com` (domaine différent, non mentionné dans cette demande), Flutter apps (`Flutter APP/{User,Agent,Merchant}` référencent aussi `ovosolution.com`/`com.ovosolution.*` dans leurs identifiants de package, config Firebase et `environment.dart` — hors périmètre car (a) la demande visait explicitement « le site web » et (b) la règle « jamais modifier le dossier Flutter » s'applique sans exception ici, contrairement à Laravel.
- `2026-07-30` — Configuration directe de l'app Flutter User (exception validée par l'utilisateur, décision de mener Flutter et React Native en parallèle — voir règle 2 ci-dessus) :
  - `lib/environment.dart` : `appName` → `AMANA`, `LIVE_API_URL`/`TEST_API_URL` → `https://fintech.gorapene.com` (vrai backend Amana, vérifié en ligne via `GET /api/general-setting` : `site_name: Amana`, `base_color: 2b5bee`, `maintenance_mode: 0`).
  - Libellé affiché : `AMANA` dans `AndroidManifest.xml` (`android:label`) et `ios/Runner/Info.plist` (`CFBundleDisplayName`).
  - Package Android / bundle iOS renommés `com.ovosolution.ovopay` → `com.amanabank.clientapp` (demandé explicitement par l'utilisateur) : `android/app/build.gradle.kts` (namespace + applicationId), `MainActivity.kt` déplacé vers `android/app/src/main/kotlin/com/amanabank/clientapp/`, `android/app/google-services.json` (`package_name`), `ios/Runner.xcodeproj/project.pbxproj` (6 occurrences `PRODUCT_BUNDLE_IDENTIFIER`), `lib/firebase_options.dart` (`iosBundleId`).
  - Firebase reconfiguré sur le vrai projet utilisateur **amana-ff5e8** (fourni par l'utilisateur, fichiers `google-services.json` Android et `GoogleService-Info.plist` iOS) : `android/app/google-services.json` remplacé intégralement, `lib/firebase_options.dart` (blocs `android` et `ios` : apiKey/appId/messagingSenderId/projectId/storageBucket), `firebase.json`, `GoogleService-Info.plist` ajouté dans `ios/Runner/` et câblé dans `project.pbxproj` (PBXFileReference + PBXBuildFile + groupe Runner + phase Resources, car pas d'Xcode disponible sur Windows pour le faire via l'IDE).
  - **Non fait, en attente** : logo/icône réels (l'utilisateur a envoyé le fichier logo AMANA dans le chat à deux reprises, mais aucune image de conversation collée ne peut être capturée comme fichier par les outils disponibles ici — il faut un chemin de fichier sur disque, comme cela a été fait pour les fichiers Firebase). Reconfiguration Firebase iOS **native** (l'app iOS elle-même reste à valider en conditions réelles, jamais buildée/testée ici, pas d'environnement macOS/Xcode disponible).
  - `flutter pub get` et `flutter analyze` passent sans erreur après tous ces changements.
- `2026-07-30` — Refonte complète des templates de notification en français, habillage visuel professionnel AMANA (exception validée explicitement par l'utilisateur, périmètre = contenu des templates + template email global, pas de logique métier) :
  - Contexte technique découvert en auditant `app/Notify/NotifyProcess.php` : le template email **global** (`general_settings.email_template`, éditable dans `admin/notification/global/email`) sert de **layout maître** — chaque template d'événement (`notification_templates.email_body`) est injecté dedans via le short-code `{{message}}`. Un seul habillage visuel (logo, couleurs, pied de page) à maintenir, pas un par événement.
  - `install/database.sql` modifié via un script Node.js dédié (tokenizer SQL maison respectant guillemets/échappements, car les lignes `INSERT` font plusieurs Ko chacune) :
    - `general_settings` : `email_template` (nouveau layout HTML — dégradé bleu Amana `#2B5BEE`→`#173583`, carte blanche arrondie, section logo, pied de page marine), `sms_template`, `push_template`, `push_title` — tous réécrits en français.
    - `notification_templates` : les **48 templates par événement** (sur 49 — `DEFAULT` volontairement inchangé car c'est un pur passe-plat `{{subject}}`/`{{message}}` sans texte à traduire) réécrits en français professionnel (`subject`, `email_body`, `sms_body`, `push_title`, `push_body`), en réutilisant exactement les mêmes short-codes que la version anglaise d'origine (vérifiés un par un depuis le contenu réel de chaque template, pas depuis la colonne `shortcodes` qui s'est révélée incomplète pour au moins un cas — `MONEY_REQUEST_REJECT` déclarait `{}` alors que `{{amount}}`/`{{from_user}}`/`{{time}}` sont bien utilisés en pratique).
    - Amélioration par rapport à l'original : plusieurs `push_title`/`push_body` qui étaient `null` dans le seed vendeur (ex. `SEND_MONEY`, `CASH_OUT`, `DONATION`, `REQUESTED_MONEY_RECEIVED`, `SEND_OTP`) ont maintenant un vrai contenu français.
    - Validation faite : re-parsing du fichier généré (49/49 lignes `notification_templates` cohérentes, 0 mismatch de colonnes), comptage `INSERT INTO`/`CREATE TABLE` identique avant/après (44 et 81, aucune autre table touchée), sauvegarde de l'original conservée (`install/database.sql.bak-<timestamp>`). Pas de serveur MySQL local disponible pour un import réel de test (WAMP arrêté) — validation limitée à la cohérence syntaxique/structurelle du fichier, pas à un import MySQL réel.
  - **Logo dans l'email** : en l'absence du fichier logo (voir point Flutter ci-dessus), le bandeau logo utilise un texte stylisé « AMANA » en CSS (`master_templates.js` conserve une version `email_template_raw` avec un point d'insertion `LOGO_IMG_PLACEHOLDER` pour swap ultérieur en `<img>` — base64 recommandé pour éviter toute dépendance à un hébergement externe non garanti). À refaire dès que le fichier logo est disponible sur disque.
  - **Précision** : `notification_templates` est une table **unique et partagée** par toute la plateforme (User + Agent + Merchant) — confirmé par la présence dans les 48 templates traités de codes clairement agent/merchant (`CASH_IN_AGENT`, `CASH_IN_COMMISSION_AGENT`, `CASH_OUT_COMMISSION_AGENT`, `CASH_OUT_TO_AGENT`, `MAKE_PAYMENT_RECEIVE`). Les notifications Agent/Merchant sont donc déjà couvertes par ce même travail, pas besoin d'un passage séparé.
- `2026-08-01` — Remplacement littéral « MFS » → « AMANA FINTECH » (demande initiale de l'utilisateur), puis reformulation complète en contenu marketing français, ton « Djamo mais pour le Gabon » (deuxième demande de l'utilisateur, même session) — exception validée explicitement, périmètre = contenu affiché (SEO, blog, onboarding, descriptions admin), pas de logique métier :
  - `install/database.sql` modifié via deux scripts Node.js dédiés successifs (même approche tokenizer que le 2026-07-30) :
    - **6 articles de blog** (`frontends` / `blog.element`, ids 178-183, génériques sur « l'industrie MFS ») entièrement réécrits en français, contextualisés Gabon (Libreville, Port-Gentil, Franceville, BEAC/CEMAC, diaspora, agents Amana) plutôt que traduits littéralement — titres, corps HTML complet (structure `<h4>` conservée), et slugs (`the-rise-of-mfs-...` → `l-essor-des-paiements-mobiles-au-gabon-...`) tous changés.
    - **SEO/social sitewide** (`frontends` id=1, `seo.data`) : `social_title`, `description`, `social_description`, `keywords` réécrits en français, positionnement Gabon.
    - **FAQ** « What is Amana? » (id 190) traduite en français (seule entrée FAQ touchée sur 14 — les 13 autres restent en anglais, non demandé de les traduire).
    - **`module_settings`** : les 17 lignes (`description_disabled`/`description_enabled`, USER + AGENT, y compris Gift Card et Investment qui sont des `INSERT` séparés) traduites en français — c'est ce qui alimente `/admin/module-setting`. Les `title` (« Send Money », « Cash Out »...) volontairement laissés en anglais (non demandé).
  - **Bug d'échappement découvert et corrigé en cours de route** : cette table `frontends` stocke ses colonnes JSON avec un **double niveau d'échappement** — `"` a un seul backslash (`\"`) mais `/` et les retours ligne (`\r\n`) en ont deux (`\\/`, `\\r\\n`), conséquence du JSON déjà échappé une fois par `json_encode()` PHP puis ré-échappé par l'export SQL. Le premier script n'appliquait qu'un seul niveau sur `/`/`\r\n`, ce qui aurait cassé le JSON stocké (contenu HTML avec `<\/p>`, `<br \/>` etc. mal échappé). Diagnostiqué via inspection `JSON.stringify` des octets bruts d'une ligne non touchée (`policy_pages.element`), puis corrigé par un second script qui relocalise les 6 lignes de blog via leur nouveau slug et ré-écrit `title`/`description` avec le niveau d'échappement correct. **Toujours valider par un JSON.parse aller-retour après toute modification de cette table `frontends`**, pas seulement un diff visuel — l'erreur ne saute pas aux yeux dans un éditeur de texte.
  - Validation faite : les 6 lignes blog + `seo.data` re-décodés avec succès en JSON valide après correction ; comptage `INSERT INTO`/`CREATE TABLE` identique avant/après (42 et 81, aucune autre table touchée) ; sauvegardes conservées (`install/database.sql.bak-20260801143827-pre-gabon-rewrite` avant cette passe, plus les backups des passes précédentes). Pas de serveur MySQL local pour un test d'import réel — validation limitée à la cohérence syntaxique du fichier.
  - **Côté Flutter User** (`lib/core/utils/my_strings.dart`) : les 3 écrans d'onboarding (`onboardTitle1-3`/`onboardSubtitle1-3`) traduits en français, même ton Amana/Gabon — **seuls ces 6 strings ont été traduits**, le reste du fichier (~550 lignes) reste en anglais. Une localisation FR complète de l'app Flutter n'a pas été demandée et n'a pas été faite ; à traiter séparément si besoin, pour éviter un mélange EN/FR dans le reste du parcours utilisateur.
  - `pubspec.yaml`/`README.md` des 3 apps Flutter (User/Merchant/Agent) : description one-liner reformulée (« Amana [Agent/Marchand] — L'application fintech mobile 100% gabonaise »).
  - **Non traité, connu et non bloquant** : les 8 fichiers `core/resources/lang/{bn,es,fr,it,ja,pt,ru,tr}.json` contiennent encore des entrées de cache de traduction avec l'ancien texte anglais « ...AMANA FINTECH... » (clé = ancien texte source anglais des blogs, désormais remplacés en base par du français natif). Ces entrées sont orphelines/inertes — leur clé ne correspond plus à aucun contenu affiché — mais n'ont pas été nettoyées (repérées après coup, impact nul sur le rendu, purge cosmétique seulement).
- `2026-08-01` — Langue par défaut de l'app User passée à français, format de montant changé pour « 1 000 fcfa » (espace milliers, sans décimales, symbole après en minuscule), puis génération de l'APK release — exception validée explicitement, périmètre = config/formatage d'affichage, pas de logique métier :
  - `Flutter APP/User/lib/core/data/controller/localization/localization_controller.dart` : `myLanguages` réordonné, français (`fr`/`GA`) en position 0 (= langue par défaut au premier lancement, via `SharedPreferenceService` + `fallbackLocale` dans `main.dart` qui lit dynamiquement `LocalizationController().locale`). `install/database.sql` (table `languages`) : flag `is_default` déplacé de l'anglais (id 1) vers le français (id 15), pour cohérence admin/API.
  - Format monétaire : nouvelle fonction de regroupement par espaces (`_groupThousandsWithSpace` dans `util.dart`, `_groupThousands` dans `string_format_helper.dart`) branchée dans les deux fonctions de formatage centrales (`MyUtils.getUserAmount`, `AppConverter.formatNumber`), donc app-wide par construction. Symbole déplacé après le montant, en minuscule, sur ~15 écrans qui construisaient leur propre affichage en bypassant ces fonctions centrales (solde dashboard, investissement — overview + history, relevés, factures, recharge crédit) : tous corrigés individuellement pour retomber sur le même format.
  - `install/database.sql` (`general_settings`) : devise passée de USD/`$` à XAF/`FCFA`, `operating_country` de United States à Gabon (`+241`, 8 chiffres — **valeur non certifiée à 100%, la réforme de numérotation gabonaise de 2021 n'a pas pu être vérifiée avec certitude, à confirmer si l'inscription bloque des utilisateurs réels**), `allow_precision` 2→0 (FCFA n'a pas de sous-unité), `thousand_separator` `,`→` ` (ce champ n'est en réalité pas consommé côté Flutter, le regroupement est géré en dur dans le code Dart — changé quand même pour la cohérence admin).
  - Validation : `flutter analyze` → 0 issue ; `flutter build apk --release` → succès, `build/app/outputs/flutter-apk/app-release.apk` (94,2 Mo).
- `2026-08-01` — Ajout d'un code OTP secret de secours (« TEST_OTP_BYPASS_CODE »), accepté en plus du vrai OTP SMS/email lors de la vérification d'inscription uniquement, pour permettre les tests internes et la revue Google Play (qui ne peut pas recevoir de SMS sur ses téléphones de test) sans désactiver l'OTP réel pour les utilisateurs — demande initiale de l'utilisateur pour un bypass universel refusée (aurait cassé la sécurité OTP pour tous les comptes réels d'une app qui manipule de l'argent), cette version scoped/secrète proposée et validée à la place :
  - `core/config/services.php` : nouvelle clé `services.test_otp_bypass_code` ← `env('TEST_OTP_BYPASS_CODE')`.
  - `core/app/Http/Controllers/Api/AuthorizationController.php` : `mobileVerification()` et `emailVerification()` acceptent désormais `$user->ver_code == $request->code` **OU** le code de test (nouvelle méthode `isTestOtpCode()`, comparaison stricte en string, retourne `false` si la config est vide — donc désactivé par défaut tant que la variable d'env n'est pas définie côté serveur). Le flux OTP réel (génération, envoi SMS/email, expiration) est totalement inchangé. Volontairement **pas** appliqué à `VerificationProcessController::verifyOtp` (OTP transactionnel : mot de passe oublié, confirmations de paiement) ni à `g2faVerification` (2FA app, pas SMS) — hors périmètre de la demande (« inscription ou connexion »), pour limiter la surface touchée.
  - Code généré : `476124` (aléatoire, stocké uniquement dans `core/.env`, qui est gitignored — donc jamais commité). **Ce `.env` local (vide, `DB_DATABASE` non renseigné) n'est probablement pas celui du serveur de production `fintech.gorapene.com`** — il faut ajouter manuellement la ligne `TEST_OTP_BYPASS_CODE=476124` dans le `.env` du serveur en production pour que ça prenne effet là où l'app réelle tape l'API. Fichiers à uploader sur le serveur : `app/Http/Controllers/Api/AuthorizationController.php` et `config/services.php` (+ la ligne `.env` à ajouter à la main, ce fichier n'étant jamais transféré).
  - Validation : `php -l` propre sur les deux fichiers PHP modifiés.
- `2026-08-01` — Bug critique trouvé et corrigé dans `install/database.sql`, domaine du backend changé `fintech.gorapene.com` → **`amanagroupe.com`**, et nettoyage de `install/` (rapporté par l'utilisateur : erreur MariaDB #1064 à l'import) :
  - **Cause racine** : le script Node.js du 2026-08-01 (rewrite Gabon/Djamo) qui corrigeait l'échappement des `/` et `\r\n` dans les 6 articles de blog a, par erreur, régénéré `title`/`description` avec une fonction d'échappement **qui omettait l'échappement des apostrophes** (`'`). Résultat : chaque apostrophe française (« L'avenir », « l'essor »...) fermait prématurément la chaîne SQL délimitée par `'...'`, cassant la syntaxe dès le premier article — exactement l'erreur #1064 reçue par l'utilisateur.
  - **Correction** : nouveau script (`fix_apostrophes.js`) régénérant les 6 lignes `blog.element` avec une fonction d'échappement complète (quote simple `\'`, quote double `\"`, slash `\\/`, retours ligne `\\r\\n`, backslash `\\\\`).
  - **Changement de méthode de validation, important pour la suite** : la validation précédente (JSON.parse sur le fragment extrait) était **aveugle à ce type de bug** — un guillemet simple non échappé reste un JSON valide (les apostrophes ne sont pas spéciales en JSON), donc ça ne cassait que la couche SQL, jamais testée. Cette fois, validation par **import réel** dans une instance MariaDB 11.4.9 temporaire (démarrée localement via `wamp64/bin/mariadb`, base de test `amana_import_test`, port 3307, arrêtée et supprimée après coup) : `mysql ... < database.sql` → exit code 0, 78 tables créées, contenu des 6 articles relu depuis la base et re-vérifié en JSON valide. C'est le seul test qui aurait détecté le bug initial — **à refaire systématiquement après toute modification de `database.sql` contenant du texte avec apostrophes**, ne plus se fier à une validation JSON seule.
  - `Flutter APP/User/lib/environment.dart` : `LIVE_API_URL`/`TEST_API_URL` → `https://amanagroupe.com`. En cohérence avec le travail Gabon/français déjà fait, remis à niveau au passage (constantes de repli seulement, la vraie valeur vient de l'API `general-setting`) : `defaultLangCode`/`defaultLanguageName` → `fr`/`Français`, `defaultPhoneDialCode`/`defaultCountryCode` → `241`/`GA`, `maxMobileNumberDigit` → 8 (cohérent avec `operating_country` déjà changé côté `general_settings` le 2026-08-01).
  - **Vérifié en direct** : `amanagroupe.com` répond (DNS/serveur OK) mais `GET /api/general-setting` renvoie une exception SQL — la base `vuwa6471_ocash` de ce serveur n'a pas encore ses tables (`general_settings` n'existe pas), donc **le site n'est pas encore installé à cette adresse**. `/install` renvoie une 500 (pas pu diagnostiquer plus, pas d'accès SSH/FTP à ce serveur). L'utilisateur doit installer/importer `database.sql` sur ce serveur avant que l'app pointant vers `amanagroupe.com` fonctionne réellement.
  - `install/` nettoyé à la demande explicite de l'utilisateur (« un seul fichier sql à jour ») : suppression des 3 sauvegardes (`database.sql.bak-*`) et de `notification_templates_fr_update.sql` (script incrémental du 2026-07-30 pour mettre à jour une base déjà installée sans réinstaller — son contenu était déjà intégralement repris dans `database.sql`, donc redondant pour un usage d'installation fraîche). Il ne reste que `install/database.sql`, maintenant validé par import réel.
- `2026-08-02` — Poussée du dépôt vers GitHub (branche `main`), puis correctifs demandés par l'utilisateur : bug KYC (chargement infini sur mobile), traduction manquante du formulaire d'inscription, refonte visuelle de la carte solde/QR + animation dépliante des services, et configuration de PawaPay avec devise auto-détectée par pays :
  - **Push GitHub** : `.gitignore` racine modifié (`/Flutter APP` → exclusion ciblée `Flutter APP/Agent` + `Flutter APP/Merchant` seulement, `Flutter APP/User` désormais suivi — ses `.gitignore` imbriqués déjà corrects, standard Flutter, excluent `build/`, `.dart_tool/`, `.gradle`, keystores). Vérifié avant commit : aucun secret dans le diff staged, aucun artefact de build (`du` hors `build/`/`.dart_tool/`/`.gradle` = 12 Mo contre 1,3 Go avec). `install/database.sql` reste hors-git (raison déjà documentée : clés API de passerelles de paiement réelles). Un fichier `Projet OCASH.zip` (131 Mo, non créé par une session Claude) trouvé à la racine, volontairement laissé hors du commit (contenu inconnu). Commit `3a98fe0`, 639 fichiers.
  - **Bug KYC (chargement infini mobile, fonctionnait sur le web)** : cause trouvée dans `ApiService.postMultiPartRequest` (`core/utils/... /api_service.dart` côté Flutter User) — `files.forEach((key, file) async {...})` n'attend jamais les callbacks async (piège classique Dart : `Map.forEach` ignore les `Future` retournés), donc la requête POST multipart partait AVANT que les fichiers ne soient réellement attachés à la `FormData`. Corrigé en remplaçant par une boucle `for...of` avec `await` explicite avant chaque `formData.files.add()`. Vérifié qu'aucun autre endroit du code n'utilise ce même anti-pattern (`forEach(... async)` introuvable ailleurs). Défense en profondeur ajoutée en plus : `KycController.submitKycData()` n'avait aucun `try/catch/finally` — une exception laissait `submitLoading` bloqué à `true` indéfiniment (l'utilisateur devait « passer l'étape »). Ajout d'un `try/catch/finally` qui garantit que le spinner se réinitialise toujours, même en cas d'erreur imprévue.
  - **Champs d'inscription non traduits malgré le français sélectionné** : cause = usage incohérent du mécanisme de traduction GetX — de nombreux `MyStrings.xxx` étaient utilisés sans le suffixe `.tr` (donc toujours en anglais, quelle que soit la langue active), alors que d'autres à côté l'avaient. Bug pervasif, pas limité à l'inscription : corrigé dans tout le dossier `lib/app/screens/auth/` (inscription/`profile_complete_screen.dart` — labels et hints de tous les champs —, connexion, biométrie, mot de passe oublié, vérification email, KYC). Seules des lignes commentées (code mort) restent sans `.tr`, volontairement ignorées.
  - **Design carte solde/QR (accueil)** : rangée de 5 boutons d'action (Add Money/Send/Cash Out/Payment/Request) supprimée de `home_screen_balance_card.dart` car strictement redondante avec la grille « Autres services » juste en dessous (méthode `buildBalanceCardActionIcon` devenue morte, supprimée aussi, ainsi que l'import `skeletonizer` devenu inutile). Améliorations visuelles ajoutées : ombre portée bleu-primaire sur la carte, bouton QR habillé du même style de pastille semi-transparente que le bouton œil (cohérence visuelle, il flottait seul avant), transition fondu+zoom (`AnimatedSwitcher`) sur le montant et l'icône œil au moment du toggle afficher/masquer le solde.
  - **Animation dépliante des services** (`home_screen_service_menu_card.dart`) : le bouton « Plus »/« Moins » existait déjà mais changeait le contenu de la grille de façon abrupte (`setState` sans transition). Grille enveloppée dans un `AnimatedSize` (350ms, `Curves.easeInOut`) pour un vrai effet d'accordéon, plus une rotation 180° (`AnimatedRotation`) de l'icône sur le bouton « Moins » façon chevron.
  - `flutter analyze` relancé après tous ces changements → 0 issue.
  - **PawaPay configuré avec une vraie clé API de production** (token JWT fourni par l'utilisateur dans le chat, jamais réaffiché ni loggé) — `install/database.sql` modifié via script Node.js dédié, validé par mon tokenizer SQL strict (pas de nouvelle instance MariaDB cette fois : un `mysqld` déjà en cours d'exécution sur la machine, non identifié comme m'appartenant, bloquait le dossier de données — je n'ai pas forcé son arrêt) :
    - `gateways` (id 61, PawaPay) : `supported_currencies` complété avec `XAF` (n'existait pas, seul XOF/GHS/KES/UGX/ZMW/NGN étaient prévus par le vendeur).
    - `gateway_currencies` : ligne XOF (id 335, Sénégal) mise à jour avec le vrai `api_token`, `environment: production`, `country: SN`. Nouvelle ligne XAF (id 337, Gabon) créée avec le même token, `country: GA`. Les deux ont `correspondent` volontairement laissé vide — c'est un code opérateur par pays (ex. Orange/MTN/Airtel) propre à chaque corridor PawaPay, à récupérer par l'utilisateur depuis son propre tableau de bord PawaPay ; je n'ai pas de moyen fiable de le déterminer et je préfère le laisser visiblement vide plutôt que d'inventer une valeur qui casserait silencieusement les dépôts réels.
    - **`status` du gateway volontairement laissé désactivé (2)**, pas activé automatiquement : sans les codes `correspondent` réels, activer maintenant laisserait des utilisateurs choisir PawaPay et échouer au moment du dépôt. Dernière étape manuelle pour l'utilisateur : renseigner `correspondent` pour SN et GA depuis `/admin/gateway/edit/PawaPay`, puis activer le gateway.
    - **`environment: production` choisi par défaut** (pas de certitude absolue — impossible de distinguer un token sandbox d'un token production juste en le lisant ; le token fourni a une expiration très longue (2036) et tout ce projet vise une mise en prod réelle, donc production est l'hypothèse la plus probable). Si les dépôts échouent avec une erreur d'authentification, c'est le premier réglage à vérifier/basculer en `sandbox` depuis l'admin.
    - `operating_countries` (table qui alimente le sélecteur de pays à l'inscription, ne contenait QUE « United States ») : Gabon (`GA`, +241, 8 chiffres) et Sénégal (`SN`, +221, 9 chiffres) ajoutés — sans ça, aucun utilisateur ne peut s'inscrire depuis ces pays et `users.country_code` ne vaudrait jamais `GA`/`SN`, ce qui aurait rendu le filtrage automatique ci-dessous inopérant en pratique.
  - **Devise automatique par pays** (`core/app/Http/Controllers/Api/PaymentController.php::methods()`) : nouvelle méthode privée `filterByUserCountry()` — pour toute `gateway_currency` dont le `gateway_parameter` contient un champ `country` (PawaPay aujourd'hui ; générique, s'appliquera automatiquement à tout futur gateway conçu pareil, ex. InTouch si un jour multi-pays), ne renvoie à l'app que l'entrée dont le `country` correspond au `country_code` de l'utilisateur connecté (`auth()->user()->country_code`, comparaison insensible à la casse). Les gateways non scopés par pays (Stripe, InTouch actuel à un seul corridor fixe) ne sont pas affectés — le filtre les laisse passer tels quels puisqu'ils n'ont pas de champ `country`. Résultat : un utilisateur gabonais ne voit que « PawaPay XAF », un utilisateur sénégalais ne voit que « PawaPay XOF », sans liste déroulante à choisir manuellement.
  - Validation : `php -l` propre sur `PaymentController.php` ; tokenizer SQL strict propre sur les 3 blocs modifiés de `database.sql` (comptage INSERT/CREATE TABLE inchangé : 42/81).
- `2026-08-02` — PawaPay : sélection automatique de l'opérateur mobile money selon le pays de l'utilisateur (demande de l'utilisateur : « détecter via le numéro de téléphone son pays et proposer automatiquement les opérateurs pris en compte dans ce pays »). En creusant, j'ai découvert que le formulaire admin (`/admin/gateway/edit/PawaPay`) **n'avait en réalité aucun champ pour saisir `country`/`correspondent`** — bug préexistant de l'intégration du 2026-07-29 : `ProcessController` les lisait depuis `gateway_parameter`, mais `gateways.gateway_parameters` (le schéma qui génère les champs du formulaire) ne définissait que `api_token`/`environment`, donc rien n'était éditable via l'admin. Corrigé au passage.
  - **Précision sur « détecter via le numéro de téléphone »** : le pays de l'utilisateur est déjà déterminé à l'inscription via `operating_countries` (sélecteur de pays qui fixe `dial_code` + `country_code` + valide le format du numéro pour ce pays) — le filtrage par pays ajouté le 2026-08-01 (`filterByUserCountry`) s'appuie déjà sur cette donnée, qui est donc bien indirectement liée au numéro de téléphone de l'utilisateur. Je n'ai pas ajouté de parsing de numéro séparé (aurait été redondant avec ce qui existe déjà) — seule la nouveauté ci-dessous (sélection de l'opérateur) manquait.
  - `install/database.sql` (script Node.js dédié, validé par le même tokenizer SQL strict, pas de nouvelle instance MariaDB pour la même raison que la fois précédente — process `mysqld` tiers toujours actif sur la machine) :
    - `gateways` (id 61) : `gateway_parameters` étendu avec deux nouveaux champs **non-globaux** `country` et `correspondents` (donc désormais un champ par devise, comme `min_amount`/`rate`/etc. — visibles et éditables dans le formulaire admin). `api_token`/`environment` (globaux) également mis à jour avec la vraie valeur pour que le formulaire admin les affiche pré-remplis s'il est rouvert.
    - `gateway_currencies` : le champ `correspondent` (une seule valeur) remplacé par `correspondents` (tableau JSON `[{"name":...,"code":...}]`) sur les deux lignes existantes — pré-rempli avec les noms d'opérateurs mobile money réellement actifs dans chaque pays (connaissance générale, pas de code PawaPay inventé, `code` laissé vide pour chaque entrée) : Sénégal (335) → Orange Money, Free Money, Wave ; Gabon (337) → Airtel Money, Moov Money. L'utilisateur doit encore renseigner le `code` PawaPay exact de chaque opérateur (depuis son tableau de bord/la doc PawaPay) via le formulaire admin désormais fonctionnel.
  - `core/app/Http/Controllers/Api/PaymentController.php` :
    - Nouvelle méthode privée `operatorNames($currency)` : lit `gateway_parameter->correspondents` (tolère qu'il soit stocké en tableau JSON réel ou en chaîne JSON-encodée — ce sera une chaîne après un premier aller-retour par le formulaire admin, puisque c'est un champ texte), renvoie uniquement les `name` (jamais les `code`, qui restent côté serveur).
    - `methods()` : attache désormais `operators` (liste de noms) à chaque entrée retournée à l'app.
    - `depositInsert()` : accepte un paramètre `operator` optionnel ; si la devise a des opérateurs configurés, le rend obligatoire et vérifie qu'il correspond à un nom connu (sinon `invalid_operator`) ; stocke le choix dans `Deposit::detail` (colonne texte existante, inutilisée jusqu'ici) en JSON `{"operator":"..."}`.
  - `core/app/Http/Controllers/Gateway/PawaPay/ProcessController.php` : nouvelle méthode privée `resolveCorrespondent()` — relit l'opérateur choisi depuis `Deposit::detail`, retrouve son `code` dans la liste `correspondents` de la devise, et l'utilise dans l'appel à l'API PawaPay (au lieu de l'ancien champ fixe `correspondent`). Reste compatible avec l'ancien format à opérateur unique (`correspondent`) si jamais réutilisé ailleurs. Si aucun `code` n'est configuré pour l'opérateur choisi, le dépôt échoue proprement avec un message clair (« opérateur pas encore configuré, contactez le support ») plutôt que d'envoyer une requête invalide à PawaPay.
  - **Côté Flutter User** : `DepositMethods` (modèle) — nouveau champ `operators` (liste de noms). `AddMoneyController` — `selectedOperator` (réinitialisé à chaque changement de méthode de paiement), `setOperator()`, validation dans `submitDeposit()` (bloque si des opérateurs existent pour la méthode choisie mais qu'aucun n'est sélectionné), `operator` transmis à `AddMoneyRepo.insertDeposit()`. `add_money_amount_page.dart` — nouveau sélecteur d'opérateurs (chips, même composant que les montants rapides), visible uniquement si la méthode choisie a des opérateurs.
  - Validation : `php -l` propre sur les 2 fichiers PHP modifiés, tokenizer SQL strict propre sur les 3 blocs modifiés, `flutter analyze` → 0 issue.
- `2026-08-02` — Migration PawaPay v1→v2 + données d'opérateurs réelles + automatisation de la saisie, suite au lien de doc fourni par l'utilisateur (`docs.pawapay.io/v2/docs/deposits`) et sa demande explicite « je veux la saisie des opérateurs automatique » :
  - **Découverte critique en lisant la doc v2** : le token API fourni par l'utilisateur est un token **sandbox**, pas production (je l'avais mis en `production` par défaut le 2026-08-01 faute de certitude — corrigé). Confirmé en appelant `GET /v2/active-conf` en direct avec ce token : `https://api.pawapay.io` (prod) → 401 `AUTHENTICATION_ERROR` ; `https://api.sandbox.pawapay.io` → 200, `companyName: "AMANA PAY"`.
  - **Données réelles récupérées en direct depuis le compte PawaPay de l'utilisateur** (appel `GET /v2/active-conf?country={GAB|SEN}&operationType=DEPOSIT`), remplaçant mes noms/codes d'opérateurs devinés du 2026-08-02 précédent : **Gabon → Airtel uniquement (`AIRTEL_GAB`)**, pas de Moov configuré sur ce compte. **Sénégal → Orange (`ORANGE_SEN`) et Free (`FREE_SEN`)**, pas de Wave configuré. `install/database.sql` mis à jour avec ces codes vérifiés (plus aucun code vide à compléter par l'utilisateur pour ces deux pays).
  - **⚠️ Point bloquant repéré, à corriger par l'utilisateur, pas par moi** : la réponse `active-conf` montre `callbackUrl: https://www.cashkap.xyz/payment/pawapay/webhook` sur les deux pays — l'URL de callback configurée côté PawaPay pointe vers un **autre domaine** (`cashkap.xyz`), pas `amanagroupe.com`. Tant que ce n'est pas changé depuis le tableau de bord PawaPay (ou leur API de config), les callbacks de dépôt n'arriveront jamais sur `ipn()` et les dépôts resteront bloqués en « pending » indéfiniment, même avec tout le reste correctement configuré.
  - **Migration v1 → v2 de `core/app/Http/Controllers/Gateway/PawaPay/ProcessController.php`** (l'intégration du 2026-07-29 utilisait déjà des endpoints/champs v1, jamais mis à jour) :
    - `POST /deposits` → `POST /v2/deposits`.
    - Payload restructuré : `payer.type` `MSISDN`→`MMO`, `payer.address.value`→`payer.accountDetails.phoneNumber`, `correspondent` (racine)→`payer.accountDetails.provider`, `statementDescription`→`customerMessage` (contrainte v2 : 4-22 caractères alphanumériques/espaces uniquement, donc filtré avec un `preg_replace`), champs `country` (racine) et `customerTimestamp` supprimés (plus nécessaires en v2, le provider suffit).
    - Statuts de réponse à l'appel initial : `ACCEPTED | REJECTED | DUPLICATE_IGNORED` (v1 avait `ENQUEUED`, disparu en v2).
    - `ipn()` : payload lu de façon tolérante (plat ou sous `data.*`, la doc publique v2 ne montre pas le schéma exact du callback — seul le format de la réponse de `check-deposit-status` est documenté, dont je pars du principe qu'il partage la même forme).
  - **Automatisation demandée par l'utilisateur — nouvelle fonctionnalité « Sync operators from PawaPay »** : `Admin\AutomaticGatewayController::syncOperators()` (nouvelle route `POST admin/gateway/automatic/sync-operators/{id}`) — appelle `GET /v2/active-conf` en direct pour le pays de la devise concernée, ne garde que les providers avec `DEPOSIT.status == OPERATIONAL`, et réécrit `correspondents` avec les vrais noms/codes PawaPay. Bouton ajouté dans `admin/gateways/automatic/edit.blade.php`, visible uniquement pour le gateway PawaPay. Mapping ISO2→ISO3 ajouté (`AutomaticGatewayController::ISO2_TO_ISO3`, une quinzaine de pays africains courants — à étendre si de nouveaux `operating_countries` sont ajoutés) car `active-conf` exige l'alpha-3 alors que notre `country` interne (aligné sur `users.country_code`) est en alpha-2.
  - **Automatisation supplémentaire — prédiction de l'opérateur depuis le numéro de téléphone** (satisfait aussi la demande initiale du 2026-08-01 « détecter via le numéro de téléphone ») : `Api\PaymentController::predictOperator()` appelle `POST /v2/predict-provider` avec le numéro de l'utilisateur connecté, uniquement si la devise a plus d'un opérateur configuré (économise l'appel sinon) ; en cas de correspondance avec un `correspondents[].code` connu, expose `predicted_operator` (le nom, jamais le code) dans la réponse de `methods()`. Échec silencieux (timeout 5s, try/catch) — ne bloque jamais l'affichage des méthodes de paiement. Côté Flutter, `AddMoneyController.setPaymentMethod()` pré-sélectionne cet opérateur (reste modifiable, le sélecteur par chips n'est jamais masqué).
  - Validation : `php -l` propre sur les 3 fichiers PHP modifiés, tokenizer SQL strict propre sur les 3 blocs modifiés de `database.sql`, `flutter analyze` → 0 issue. Sauvegarde prise avant modification (`install/database.sql.bak-*-pre-v2-migration`).
- `2026-08-04` — **Bug critique corrigé** : `/admin/gateway/edit/PawaPay` renvoyait une erreur 500 en production (`htmlspecialchars(): Argument #1 ($string) must be of type string, array given`), diagnostiqué à partir de `storage/logs/laravel.log` fourni par l'utilisateur. Cause : `gateway_parameter->correspondents` était stocké comme un **vrai tableau JSON imbriqué** (`[{"name":...,"code":...}]`) depuis le 2026-08-02, alors que le template admin (`edit.blade.php`) l'affiche dans un `<input type="text" value="{{ $globalParameters->$key }}">` — Blade appelle `htmlspecialchars()` sur cette valeur, qui n'accepte qu'une chaîne, jamais un tableau.
  - **Tentatives de correction ratées avant la bonne solution, à ne pas reproduire** : j'ai d'abord essayé de stocker `correspondents` comme une **chaîne JSON imbriquée** (`"[{\"name\":...}]"`) en rejouant l'échappement SQL habituel de ce fichier (simple backslash pour les guillemets structurels, double pour les slashs/retours ligne). Plusieurs tentatives successives ont toutes échoué à l'import réel MariaDB (guillemets imbriqués sous- ou sur-échappés), y compris une validation antérieure qui avait donné un **faux positif** parce qu'elle utilisait un dé-échappement PHP fait main plutôt qu'un vrai import SQL suivi d'un vrai `SELECT`. J'ai fini par mesurer empiriquement (tables de test dédiées, valeurs à N backslashes de 0 à 5) que ce pipeline (Git Bash → `mysql.exe` client Windows → MariaDB) **ne collapse pas les paires de backslashes comme l'échappement SQL standard le laisserait supposer** — cause exacte non identifiée (spécifique à ce client/OS), mais peu importe : le format JSON imbriqué s'est révélé fondamentalement fragile ici.
  - **Solution retenue, plus robuste** : `correspondents` n'est plus du JSON du tout — c'est une chaîne texte toute simple `Nom:CODE,Nom2:CODE2` (ex. `Orange:ORANGE_SEN,Free:FREE_SEN`), **sans aucun guillemet ni accolade à l'intérieur**, donc aucune ambiguïté d'échappement possible, quel que soit l'environnement. Nouvelle méthode partagée `Api\PaymentController::parseCorrespondents()` (publique/statique, réutilisée aussi par `PawaPay\ProcessController::resolveCorrespondent()`) qui parse ce format. `Admin\AutomaticGatewayController::syncOperators()` mis à jour pour écrire dans ce même format (il avait le même bug — `$params->correspondents = $correspondents;` assignait un tableau brut). Le texte d'aide du champ dans le formulaire admin mis à jour pour décrire ce nouveau format.
  - **Leçon retenue pour la suite** : ne plus jamais valider un correctif touchant `install/database.sql` avec un dé-échappement PHP/JS fait main, même basé sur des règles déjà établies avec succès ailleurs dans ce fichier — **toujours** valider par un vrai cycle import MariaDB + vraie requête `SELECT` + vrai `json_decode()`/`htmlspecialchars()` PHP en conditions réelles avant de livrer quoi que ce soit à l'utilisateur. C'est cette méthode (répétée ~6 fois de suite jusqu'à obtenir un résultat correct) qui a permis de trouver la vraie solution après plusieurs échecs.
  - Fichiers livrés à l'utilisateur : `install/hotfix_2026-08-04.sql` (script `UPDATE` ciblé sur `gateways`/`gateway_currencies`, testé avec succès contre une base simulant l'état actuellement cassé en production) + les 3 fichiers PHP corrigés à ré-uploader (`Api/PaymentController.php`, `Gateway/PawaPay/ProcessController.php`, `Admin/AutomaticGatewayController.php`). `.gitignore` étendu (`/install/hotfix_*.sql`) — ce fichier contient aussi le token API PawaPay.
  - Diagnostiqué au passage (même session, signalement utilisateur séparé) : le bypass OTP de test (`TEST_OTP_BYPASS_CODE`) ne fonctionnait pas non plus après déploiement — cause la plus probable expliquée à l'utilisateur : soit la ligne jamais ajoutée au `.env` de production (fichier jamais transféré automatiquement, rappelé explicitement), soit un cache de config (`php artisan config:clear` / suppression de `bootstrap/cache/*.php`) jamais vidé après l'avoir ajoutée — non confirmé de mon côté, aucune erreur associée trouvée dans les logs fournis (comportement attendu : un mismatch de code OTP ne lève pas d'exception, donc rien à logger).
- `2026-08-04` — **Reconstruction complète de l'intégration PawaPay**, demande explicite de l'utilisateur : repartir de la documentation officielle v2 (`docs.pawapay.io/v2/docs/deposits`), adopter exactement le même pattern architectural que Flutterwave (`/admin/gateway` — config globale simple, aucun champ par pays/devise custom), et supprimer entièrement l'ancienne implémentation (`country`/`correspondents`, sync manuel, sélecteur d'opérateur) plutôt que de continuer à la rafistiner. Ceci rend **caduques** toutes les entrées précédentes sur `country`/`correspondents`/`resolveCorrespondent`/`syncOperators`/`predictOperator` (2026-08-02, 2026-08-04 précédent) — gardées ci-dessus pour l'historique, mais plus aucun de ces champs/méthodes n'existe dans le code actuel.
  - **Nouvelle architecture** : PawaPay n'a plus que 2 champs globaux (`api_token`, `environment`), identique à Flutterwave. Le seul concept spécifique à PawaPay — quel opérateur mobile money utiliser — n'est plus stocké nulle part : il est résolu **à chaque dépôt**, en direct, via `POST /v2/predict-provider` (endpoint officiel PawaPay conçu pour ça, prend juste le numéro de téléphone et renvoie `{country, provider}`). Zéro config locale à maintenir, zéro champ à synchroniser, zéro risque d'écart entre ce qui est stocké et ce que PawaPay accepte réellement.
  - `install/database.sql` : `gateways` (id 61) `gateway_parameters` réduit à `{api_token, environment}` (suppression des champs non-globaux `country`/`correspondents`). `gateway_currencies` (id 335 XOF, id 337 XAF) `gateway_parameter` réduit à `{api_token, environment}`. `description` du gateway (colonne texte admin) réécrite pour ne plus mentionner de codes pays/correspondant à saisir. Script Node.js dédié (`rebuild_pawapay.js`), validé par **double import MariaDB réel** (une instance de test pour le rebuild JSON, une seconde pour la validation finale après correction de `description`) + `SELECT` + `json_decode()`/`htmlspecialchars()` PHP réels — comptage `INSERT INTO`/`CREATE TABLE` inchangé (42/81).
  - `core/app/Http/Controllers/Gateway/PawaPay/ProcessController.php` : réécrit intégralement. `process()` appelle désormais `predictProvider()` (nouvelle méthode privée, `POST /v2/predict-provider`) pour obtenir `{provider, country}` à partir du numéro de l'utilisateur, puis `POST /v2/deposits` avec ce provider. Si la prédiction échoue (numéro non reconnu par PawaPay), message d'erreur clair côté utilisateur plutôt qu'un appel voué à l'échec. Ancienne méthode `resolveCorrespondent()` supprimée. `ipn()`/`status()` inchangés (déjà conformes v2).
  - `core/app/Http/Controllers/Api/PaymentController.php` : remis à sa forme simple d'origine (celle d'avant le 2026-08-01) — `methods()` ne fait plus de filtrage par pays ni n'attache `operators`/`predicted_operator` ; `depositInsert()` ne valide/stocke plus de champ `operator`. Méthodes supprimées : `filterByUserCountry()`, `parseCorrespondents()`, `operatorNames()`, `predictOperator()`.
  - `core/app/Http/Controllers/Admin/AutomaticGatewayController.php` : suppression de la constante `ISO2_TO_ISO3` et de la méthode `syncOperators()` (le bouton « Sync operators from PawaPay » n'a plus de raison d'être, il n'y a plus rien à synchroniser).
  - `core/routes/admin.php` : suppression de la route `sync-operators/{id}`. `core/resources/views/admin/gateways/automatic/edit.blade.php` : suppression du bouton correspondant.
  - **Côté Flutter User** : suppression complète du sélecteur d'opérateur (chips) devenu inutile — `add_money_amount_page.dart` (bloc UI retiré), `add_money_controller.dart` (`selectedOperator`, `setOperator()`, validation associée dans `submitDeposit()` retirés), `add_money_repo.dart` (paramètre `operator` retiré de `insertDeposit()`), `deposit_method_response_model.dart` (`operators`/`predictedOperator` retirés du modèle `DepositMethods`). `MyStrings.selectOperator` conservé car toujours utilisé par l'écran de recharge de crédit téléphonique (fonctionnalité distincte, sans rapport avec PawaPay).
  - Validation complète : `php -l` propre sur les 3 fichiers PHP modifiés ; `flutter analyze` → 0 issue sur `lib/app/screens/add_money`, `lib/core/data/repositories/add_money`, `lib/core/data/models/deposit` ; deux imports MariaDB réels indépendants (instances jetables `mariadb-install-db` + `mysqld --skip-grant-tables`, supprimées après coup) confirmant que le fichier s'importe proprement et que `json_decode()`/`htmlspecialchars()` PHP ne lèvent plus d'erreur sur les lignes PawaPay — c'est exactement le chemin qui avait causé le bug 500 du 2026-08-04 précédent, désormais éliminé par construction (plus aucun tableau imbriqué stocké dans un champ affiché tel quel par le formulaire admin).
  - **Reste à faire côté utilisateur** : re-uploader sur le serveur de production les fichiers listés ci-dessus (3 PHP + `edit.blade.php` + `routes/admin.php`), et appliquer un `UPDATE` équivalent sur `gateways`/`gateway_currencies` en base de production (la base déjà installée n'a pas les changements de schéma faits ici dans `install/database.sql`, qui ne sert qu'à une installation fraîche) — un script `UPDATE` ciblé reste à fournir avant mise en prod. Le point bloquant déjà identifié le 2026-08-02 (callback URL PawaPay pointant vers `cashkap.xyz` au lieu de `amanagroupe.com`) reste non résolu, toujours à corriger par l'utilisateur depuis le tableau de bord PawaPay.
- `2026-08-04` — **Bascule du flux PawaPay vers le Payment Page hébergé** (`docs.pawapay.io/v2/docs/payment_page`), suite au lien de doc partagé par l'utilisateur juste après la reconstruction ci-dessus. Ceci **rend caduque la description de `process()` de l'entrée précédente** (qui utilisait `POST /v2/deposits` en push serveur direct) — remplacée par le flux ci-dessous. La config DB (`gateways`/`gateway_currencies` — juste `api_token`/`environment`) ne change pas.
  - **Pourquoi ce changement** : l'utilisateur avait explicitement demandé « implémenté de la même manière que Flutterwave » ; le push serveur direct (predict-provider + `/v2/deposits` + écran d'attente qui poll) est en réalité plus proche du pattern MercadoPago que Flutterwave. Le **Payment Page** de PawaPay (`POST /v2/paymentpage` → `redirectUrl` → le client complète le paiement sur une page hébergée PawaPay → retour sur `returnUrl`) est le vrai équivalent du flux « redirection vers page hébergée » utilisé par Flutterwave/Stripe dans ce projet, et présente l'avantage de laisser PawaPay gérer lui-même la validation du numéro et la détection de l'opérateur (plus besoin d'appeler `predict-provider` pour choisir l'opérateur).
  - `core/app/Http/Controllers/Gateway/PawaPay/ProcessController.php` réécrit une seconde fois : `process()` appelle désormais `POST /v2/paymentpage` (payload `depositId`, `returnUrl`, `msisdn`, `amount`, `country`, `reason`) et retourne `{"redirect":true,"redirect_url":<redirectUrl PawaPay>}` — un raccourci **déjà générique et largement utilisé** dans `Gateway\PaymentController::depositConfirm()` (`if (isset($data->redirect)) { return redirect($data->redirect_url); }`, le même mécanisme que MercadoPago/Coingate/InTouch/BTCPay/etc.), donc **aucune vue Blade PawaPay n'est plus nécessaire** et **aucun changement côté Flutter** : le WebView générique existant (`MyWebViewWidget`, détection de succès/échec par correspondance exacte d'URL contre `successUrl`/`failedUrl`) fonctionne déjà tel quel pour ce pattern, exactement comme pour Flutterwave.
    - `amount` fixé sur la page hébergée nécessite obligatoirement `country` (ISO alpha-3, contrainte documentée de `/v2/paymentpage`) — sans ça, le client pourrait saisir un montant différent de celui calculé par l'app (frais/taux déjà appliqués), désynchronisant le grand livre. Résolu sans aucune config locale : nouvelle méthode privée `predictCountry()` réutilise `POST /v2/predict-provider` (déjà utilisé dans la version précédente) mais ne garde que le champ `country` de la réponse — le `provider` est ignoré puisque c'est désormais PawaPay lui-même qui gère le choix de l'opérateur sur sa page hébergée.
    - Nouvelle méthode publique `returnFromPaymentPage($depositId)` (route `GET ipn/pawapay/return/{depositId}`, nom `ipn.PawaPay.return`, remplace l'ancienne route `pawapay/status/{depositId}` supprimée) : point d'atterrissage du navigateur après le `returnUrl` PawaPay. Comme PawaPay ne fournit qu'un seul `returnUrl` (pas de distinction succès/échec dans l'URL, contrairement à Flutterwave qui passe `{type}` dans son URL de callback), le statut réel est vérifié en direct via `GET /v2/deposits/{depositId}` (endpoint officiel de consultation de statut) avant de rediriger vers `success_url` ou `failed_url` — fonctionne que le webhook asynchrone `ipn()` soit déjà arrivé ou non.
    - Ancienne méthode `status()` (endpoint de polling JSON, plus utilisée puisqu'il n'y a plus d'écran d'attente côté app) supprimée, ainsi que les deux vues `resources/views/templates/basic/{user,agent}/payment/PawaPay.blade.php` (l'ancien écran d'attente avec polling JS — devenues du code mort, plus jamais atteintes puisque `depositConfirm()` court-circuite directement vers `redirect()` avant tout rendu de vue).
    - `ipn()` (callback webhook serveur-à-serveur, indépendant du `returnUrl` navigateur) inchangé.
  - `install/database.sql` : seule la `description` du gateway (colonne texte admin) mise à jour pour refléter le nouveau flux hébergé (ne mentionne plus `predict-provider`/détection auto d'opérateur par l'app, mais la page hébergée PawaPay). Aucun autre champ changé — revalidé par import MariaDB réel (comptage INSERT/CREATE TABLE inchangé, 42/81).
  - `install/hotfix_2026-08-04-pawapay-rebuild.sql` mis à jour avec la même nouvelle description, re-testé avec succès contre une simulation de l'état actuel de production (import du backup pré-rebuild + application du hotfix + vérification), pour rester le script de référence à exécuter en prod pour ce chantier PawaPay dans son ensemble (les deux passes du 2026-08-04 combinées).
  - Validation : `php -l` propre sur `ProcessController.php`/`routes/ipn.php` ; aucun fichier Flutter touché dans cette passe (le WebView générique n'a pas besoin de changement) ; recherche exhaustive de références mortes (`PawaPay.status`, `status_url`, anciennes vues) → aucune trouvée en dehors d'InTouch (gateway distinct, non concerné).
  - **Reste à faire côté utilisateur** : identique à l'entrée précédente (re-upload des fichiers PHP/routes vers la prod + exécution du hotfix SQL), plus le fichier `ProcessController.php` re-livré une seconde fois avec ce nouveau contenu (écraser la version précédente). Le point bloquant callback URL (`cashkap.xyz`) reste également d'actualité pour le webhook `ipn()` — sans rapport avec ce changement de `returnUrl`, qui est une route distincte gérée entièrement côté navigateur.
